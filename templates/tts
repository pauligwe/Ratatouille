<div id="recipesSection" class="flex flex-col items-center gap-4 mt-10">
  ... (existing recipe content) ...
  <button id="speakButton" class="text-xl text-[#435238] mt-4 flex gap-2 items-center justify-center">
    <span>Speak Recipe</span>
    <i class="fa-solid fa-microphone"></i>
  </button>
</div>

// ... your existing code ...

const speakButton = document.getElementById('speakButton');
const recipeText = document.getElementById('recipe').innerText; // Get recipe text (modify if needed)

speakButton.addEventListener('click', () => {
  const synth = window.speechSynthesis;
  const utterance = new SpeechSynthesisUtterance(recipeText);
  synth.speak(utterance);
});
